# odin-recipes
The Odin-Project Recipes Challenge


The 1st HTML challenge provided by TOP 

- Setup a reomote repo on github, set to public and copy the ssh key to the terminal. 
- Clone the repo using git clone git@github.com:username/odin-recipes.git (use SSH).
- Setup the README.md file (code .) while within the directory to open vsc.
- Create an index.html file (Remeber the HTML boilerplate?)